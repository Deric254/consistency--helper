"""
DEPRECATED: `dashboard.py` has been renamed to `app.py`.

Run Streamlit against `app.py` instead:

    streamlit run app.py

This file intentionally exits to avoid duplicate UI runs if someone
accidentally runs the old filename.
"""

import sys

print("dashboard.py has been renamed to app.py. Run: streamlit run app.py")
sys.exit(0)